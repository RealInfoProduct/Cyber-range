import { ExercisefilterPipe } from './exercisefilter.pipe';

describe('ExercisefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExercisefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
