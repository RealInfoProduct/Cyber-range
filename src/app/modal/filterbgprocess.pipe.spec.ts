import { FilterbgprocessPipe } from './filterbgprocess.pipe';

describe('FilterbgprocessPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbgprocessPipe();
    expect(pipe).toBeTruthy();
  });
});
