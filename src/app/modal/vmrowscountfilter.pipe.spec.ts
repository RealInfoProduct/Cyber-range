import { VmrowscountfilterPipe } from './vmrowscountfilter.pipe';

describe('VmrowscountfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VmrowscountfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
