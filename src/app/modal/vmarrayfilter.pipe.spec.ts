import { VmarrayfilterPipe } from './vmarrayfilter.pipe';

describe('VmarrayfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VmarrayfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
